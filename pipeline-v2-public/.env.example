# ── Server ────────────────────────────────────────────────────────────
PORT=4000
PIPELINE_API_KEY=your-secure-api-key-here

# ── Database ───────────────────────────────────────────────────────────
# PostgreSQL connection string (Supabase, Railway, or self-hosted)
DATABASE_URL=postgresql://user:password@host:5432/shipOrSkip
# Optional: direct connection for migrations (Supabase pooler workaround)
DIRECT_URL=postgresql://user:password@host:5432/shipOrSkip

# ── Data Sources ───────────────────────────────────────────────────────

# CoinGecko API — project discovery + token resolution + market data
# Free tier: 10 req/min | Demo: 30 req/min | Pro: 500 req/min
# Get key: https://www.coingecko.com/en/api/pricing
COINGECKO_API_KEY=CG-xxxxxxxxxxxxxxxxxxxx

# Moralis Web3 API — on-chain verification, holder analytics, transfer data
# Free tier: 40k CUs/day | Starter: 100k CUs/day
# Get key: https://admin.moralis.io/
MORALIS_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxxxxx

# ── AI Analysis ────────────────────────────────────────────────────────
# xAI / Grok — AI survival analysis + post-mortem + x_search for Twitter intel
# Get key: https://console.x.ai/
XAI_API_KEY=xai-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Grok model for analysis (default: grok-3-mini-fast for cost efficiency)
ANALYZER_MODEL=grok-3-mini-fast

# ── Social (Optional) ──────────────────────────────────────────────────
# Twitter API v2 Bearer Token — fallback for individual handle lookups
# (Primary Twitter intel uses Grok x_search above)
# Get token: https://developer.twitter.com/
TWITTER_BEARER_TOKEN=

# ── Scheduler ─────────────────────────────────────────────────────────
# Pipeline auto-runs every N hours (default: 12)
PIPELINE_INTERVAL_HOURS=12

# Projects to process per full run (default: 200 top + 22 emerging)
PIPELINE_PROJECT_LIMIT=200
PIPELINE_EMERGING_LIMIT=22

# ── Logging ───────────────────────────────────────────────────────────
# Log level: debug | info | warn | error
LOG_LEVEL=info
